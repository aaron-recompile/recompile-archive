# Environment
.env
.env.local
.env.*.local

# Private dev handoff (GCP / deployment specifics — never commit)
RECOMPILE-ARCHIVE-HANDOFF.md

# Local MCP client config (machine-specific absolute paths)
.mcp.json

# Python
venv/
.venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Node
node_modules/
.next/
out/
.npm/
.pnp.*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Publish lab — commit the pipeline + content, but never the secrets/cache/raw export
_publish-lab/.env
_publish-lab/_publish_state.json
_publish-lab/medium-export/
_publish-lab/*.zip
